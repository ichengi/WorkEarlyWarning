name: EarlyWarning

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:

    - name: Set up Python 3.10
      uses: actions/setup-python@v1
      with:
        python-version: 3.10
		
    - name: Check
      run: 
        python EarlyWarning.py
		
